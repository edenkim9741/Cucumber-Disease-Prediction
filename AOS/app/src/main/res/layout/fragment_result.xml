<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:background="@color/white">

    <!-- 로고 (중앙 상단) -->
    <TextView
        android:id="@+id/logoTop"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="37dp"
        android:text="QcumbeR"
        android:textSize="94.37px"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 촬영 이미지 (둥근 모서리) -->
    <androidx.cardview.widget.CardView
        android:id="@+id/imageCard"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="10dp"
        android:layout_marginStart="0dp"
        android:layout_marginEnd="0dp"
        app:cardCornerRadius="45dp"
        app:cardElevation="0dp"
        app:layout_constraintDimensionRatio="973:1281"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/logoTop">

        <ImageView
            android:id="@+id/resultImageView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitXY"
            android:contentDescription="진단 이미지" />
    </androidx.cardview.widget.CardView>

    <!-- 진단 결과 텍스트 -->
    XML
    <TextView
        android:id="@+id/diseaseNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:text="흰가루병"
        android:textSize="75px"
        android:textColor="@color/disease_yellow"
        android:includeFontPadding="false"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/imageCard" />

    <TextView
        android:id="@+id/textLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="-2dp"
        android:text="이 의심돼요"
        android:textSize="75px"
        android:textColor="@color/black"
        android:includeFontPadding="false"
        app:layout_constraintStart_toEndOf="@id/diseaseNameTextView"
        app:layout_constraintBaseline_toBaselineOf="@id/diseaseNameTextView" />
    <!--신뢰도(한 줄로 표시) -->
    <TextView
        android:id="@+id/confidenceTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:translationY="2dp"
        android:text="92%"
        android:textSize="155px"
        android:textColor="@color/disease_yellow"
        android:includeFontPadding="false"
        app:layout_constraintStart_toEndOf="@id/textLabel"
        app:layout_constraintBaseline_toBaselineOf="@id/diseaseNameTextView" />
    <!--설명(검은색) -->
    <TextView
        android:id="@+id/descriptionTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="0dp"
        android:text="잎에 3~5mm정도의 회색 균사체가 나타나다가 점차 잎 전체가 밀가루를 뿌린 것처럼 확대돼요."
        android:textSize="50px"
        android:textColor="@color/black"
        android:lineSpacingExtra="2dp"
        android:fontFamily="@font/pretendard_medium"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/confidenceTextView" />

    <!-- 🔥 자세히 알아보기 영역 (가로 배치) -->
    <LinearLayout
        android:id="@+id/detailButtonLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="0dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- 화살표 이미지 -->
        <ImageView
            android:id="@+id/detailButtonImage"
            android:layout_width="270px"
            android:layout_height="183.5px"
            android:src="@drawable/detail_button_background"
            android:contentDescription="자세히 알아보기" />

        <!-- 텍스트 컨테이너 (세로) -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:orientation="vertical">

            <!-- "스크롤하여" -->
            <TextView
                android:id="@+id/scrollText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="스크롤하여"
                android:textSize="65.53px"
                android:textColor="@color/primary_green"
                android:includeFontPadding="false"
                android:lineSpacingExtra="0dp" />

            <!-- "자세히 알아보기" -->
            <TextView
                android:id="@+id/detailText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="자세히 알아보기"
                android:textSize="94.37px"
                android:textColor="@color/dark_green"
                android:includeFontPadding="false"
                android:lineSpacingExtra="0dp" />
        </LinearLayout>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>